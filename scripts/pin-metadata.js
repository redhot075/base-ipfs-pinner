const { PinataSDK } = require("pinata-sdk");\nconst fs = require("fs");\n\nconst pinata = new PinataSDK({ pinataApiKey: "YOUR_API_KEY", pinataSecretApiKey: "YOUR_SECRET" });\n\nconst metadata = JSON.parse(fs.readFileSync("metadata.json", "utf8"));\n\nasync function pinMetadata() {\n  const result = await pinata.pinJSONToIPFS(metadata);\n  console.log("IPFS URI:", `ipfs://${result.IpfsHash}`);\n}\n\npinMetadata();\n\n// Deploy: npx hardhat run scripts/deploy.js --network baseSepolia
